export interface TestUserData {
  email: string;
  firstName: string;
  lastName: string;
  phone: string;
  birth_date?: string;
}

export interface UserProfile {
  id: string;
  first_name: string;
  last_name: string;
  main_phone: string;
}